/* const body = document.querySelector(".body2");
const width = screen.width;

function tamanhoDoCorpo() {
  body.innerWidth = width;
  body.style.width = `${width}px`;
  console.log(body.innerWidth);
}
body.addEventListener("load", tamanhoDoCorpo);
tamanhoDoCorpo(); */
